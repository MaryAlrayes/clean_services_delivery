enum Relative { relative, friend, sponser, other }

enum Nationality { saudi, nonSaudi }

enum NavigationLabelsDrawer {
  home,
  currentOrders,
  log,
  statistics,
  profile,
  settings,
  systemMethod,
  profitCalcMethod,
}

enum LogOrderType{
  fromClient,
  fromLaundry
}
